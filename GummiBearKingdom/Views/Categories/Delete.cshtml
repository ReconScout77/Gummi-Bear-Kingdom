@model GummiBearKingdom.Models.Category

@{  
    ViewData["Title"] = "Category Deletion";
}

<h1 class="text-center">Are you sure you want to delete @Html.DisplayFor(model => model.Name)?</h1>
<h5 class="text-center">(Doing so will delete all products in this category.)</h5>


@using (Html.BeginForm())
{
    <div class="d-flex justify-content-center">
        <input type="submit" value="Delete" class="btn btn-danger" />
    </div> 
}

<p class="back">@Html.ActionLink("Back to category list", "Index")</p>
